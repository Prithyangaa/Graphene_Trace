@model GrapheneTrace.Model.AdminDashboardViewModel

@{
    ViewData["Title"] = "Admin Dashboard";
<<<<<<< HEAD
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

<style>
    .gt-page {
        background: #f5f7fb;
        min-height: 100vh;
        padding: 2rem 0;
    }

    .gt-card {
        background: #ffffff;
        border-radius: 18px;
        border: 1px solid #e3e7f0;
        padding: 1.5rem;
        box-shadow: 0 8px 20px rgba(15, 23, 42, 0.05);
        transition: transform .15s ease, box-shadow .15s ease;
    }

    .gt-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 26px rgba(15, 23, 42, 0.08);
    }

    .gt-card-header {
        font-weight: 600;
        font-size: 0.95rem;
        letter-spacing: .03em;
        text-transform: uppercase;
        color: #64748b;
        margin-bottom: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .gt-large-placeholder {
        background: radial-gradient(circle at 20% 20%, #f9fafb, #e5e7eb);
        border-radius: 16px;
        height: 220px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #9ca3af;
        margin-bottom: 1rem;
        font-size: 0.9rem;
        flex-direction: column;
        gap: .35rem;
    }

    .gt-badge {
        display: inline-flex;
        align-items: center;
        gap: .35rem;
        padding: .25rem .6rem;
        border-radius: 999px;
        font-size: .75rem;
        background: #e0f2fe;
        color: #0369a1;
    }

    .gt-badge-success {
        background: #dcfce7;
        color: #166534;
    }

    .gt-badge-warning {
        background: #fef9c3;
        color: #854d0e;
    }

    .gt-stat-label {
        font-size: .78rem;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: .06em;
    }

    .gt-stat-value {
        font-weight: 600;
        font-size: 1rem;
        color: #0f172a;
    }

    .gt-link-sm {
        font-size: .8rem;
        color: #6366f1;
        cursor: pointer;
    }

    .gt-section-title {
        font-size: .85rem;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: .08em;
        margin-bottom: .75rem;
    }

    .gt-chip {
        display: inline-flex;
        align-items: center;
        gap: .25rem;
        padding: .2rem .6rem;
        border-radius: 999px;
        background: #eef2ff;
        color: #4338ca;
        font-size: .75rem;
        margin-right: .35rem;
        margin-bottom: .25rem;
    }
</style>

<div class="gt-page">
    <div class="container">

        <!-- Top bar / breadcrumb -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <div class="text-muted small">Graphene Trace</div>
                <h3 class="mt-1 mb-0">Admin Dashboard</h3>
            </div>
            <div class="d-flex align-items-center gap-3">
                <span class="gt-badge-success">
                    <i class="bi bi-activity"></i>
                    @Model.SystemStatus
                </span>
                <span class="text-muted small">
                    Last updated: @Model.LastUpdated.ToLocalTime().ToString("dd MMM yyyy HH:mm")
                </span>
            </div>
        </div>

        <!-- Top two big cards -->
        <div class="row g-4 mb-4">
            <!-- System Overview -->
            <div class="col-lg-6">
                <div class="gt-card h-100">
                    <div class="gt-card-header">
                        <span>System Overview</span>
                        <span class="gt-link-sm" onclick="location.href='/Admin/Logs'">
                            View logs <i class="bi bi-chevron-right"></i>
                        </span>
                    </div>

                    <div class="gt-large-placeholder">
                        <i class="bi bi-hdd-stack" style="font-size: 2rem;"></i>
                        <div>System Status</div>
                        <span class="gt-badge-success">
                            <i class="bi bi-check-circle-fill"></i> @Model.SystemStatus
                        </span>
                    </div>

                    <div class="row text-center">
                        <div class="col-4">
                            <div class="gt-stat-label">Open Alerts</div>
                            <div class="gt-stat-value">@Model.OpenAlerts</div>
                        </div>
                        <div class="col-4">
                            <div class="gt-stat-label">Critical</div>
                            <div class="gt-stat-value text-danger">@Model.CriticalAlerts</div>
                        </div>
                        <div class="col-4">
                            <div class="gt-stat-label">Warnings</div>
                            <div class="gt-stat-value text-warning">@Model.WarningAlerts</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- User Management -->
            <div class="col-lg-6">
                <div class="gt-card h-100">
                    <div class="gt-card-header">
                        <span>User Management</span>
                        <span class="gt-link-sm" onclick="location.href='/Admin/Users'">
                            Manage users <i class="bi bi-chevron-right"></i>
                        </span>
                    </div>

                    <div class="gt-large-placeholder">
                        <i class="bi bi-people" style="font-size: 2rem;"></i>
                        <div>User Administration</div>
                    </div>

                    <div class="row text-center">
                        <div class="col-3">
                            <div class="gt-stat-label">Total</div>
                            <div class="gt-stat-value">@Model.TotalUsers</div>
                        </div>
                        <div class="col-3">
                            <div class="gt-stat-label">Patients</div>
                            <div class="gt-stat-value">@Model.TotalPatients</div>
                        </div>
                        <div class="col-3">
                            <div class="gt-stat-label">Clinicians</div>
                            <div class="gt-stat-value">@Model.TotalClinicians</div>
                        </div>
                        <div class="col-3">
                            <div class="gt-stat-label">Admins</div>
                            <div class="gt-stat-value">@Model.TotalAdmins</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Lower 4 cards -->
        <div class="row g-4">
            <!-- Devices -->
            <div class="col-lg-3 col-md-6">
                <div class="gt-card">
                    <div class="gt-card-header">
                        <span>Devices</span>
                        <span class="gt-link-sm" onclick="location.href='/Admin/Devices'">
                            Manage all <i class="bi bi-chevron-right"></i>
                        </span>

                    </div>
                    <div class="gt-section-title">Status</div>
                    <div class="mb-2">
                        <div class="gt-stat-label">Active devices</div>
                        <div class="gt-stat-value">@Model.ActiveDevices</div>
                    </div>
                    <div>
                        <div class="gt-stat-label">Offline</div>
                        <div class="gt-stat-value text-muted">@Model.OfflineDevices</div>
                    </div>
                </div>
            </div>

            <!-- System Alerts -->
            <div class="col-lg-3 col-md-6">
                <div class="gt-card">
                    <div class="gt-card-header">
                        <span class="gt-link-sm" onclick="location.href='/Admin/SystemAlerts'">
                            View all
                        </span>

                    </div>
                    <div class="gt-section-title">Alerts</div>
                    <div class="mb-2">
                        <div class="gt-stat-label">Critical</div>
                        <div class="gt-stat-value text-danger">@Model.CriticalAlerts</div>
                    </div>
                    <div class="mb-2">
                        <div class="gt-stat-label">Warnings</div>
                        <div class="gt-stat-value text-warning">@Model.WarningAlerts</div>
                    </div>
                    <div>
                        <div class="gt-stat-label">Open</div>
                        <div class="gt-stat-value">@Model.OpenAlerts</div>
                    </div>
                </div>
            </div>

            <!-- Analytics -->
            <div class="col-lg-3 col-md-6">
                <div class="gt-card">
                    <div class="gt-card-header">
                        <span>Analytics</span>
                        <span class="gt-link-sm" onclick="location.href='/Admin/Analytics'">
                            Export <i class="bi bi-chevron-right"></i>
                        </span>


                    </div>
                    <div class="gt-section-title">Quick filters</div>
                    <div>
                        <span class="gt-chip"><i class="bi bi-graph-up"></i> Usage stats</span>
                        <span class="gt-chip"><i class="bi bi-activity"></i> System performance</span>
                        <span class="gt-chip"><i class="bi bi-clipboard-data"></i> Reports</span>
                    </div>
                </div>
            </div>

            <!-- Settings -->
            <div class="col-lg-3 col-md-6">
                <div class="gt-card">
                    <div class="gt-card-header">
                        <span>Settings</span>
                        <span class="gt-link-sm" onclick="location.href='/Admin/Settings'">
                            Open
                        </span>
                    </div>
                    <div class="gt-section-title">Sections</div>
                    <ul class="list-unstyled small mb-0">
                        <li><i class="bi bi-gear me-1"></i> System config</li>
                        <li><i class="bi bi-shield-check me-1"></i> Security</li>
                        <li><i class="bi bi-cloud-arrow-up me-1"></i> Backup &amp; restore</li>
                    </ul>
                </div>
=======

    dynamic stats = ViewBag.UserStats ?? new { TotalUsers = 0, Patients = 0, Clinicians = 0, Admins = 0 };
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<div class="container mt-5 mb-5">
    <h2 class="text-center mb-4">@ViewData["Title"]</h2>

    <div class="row g-4">

        <!-- User Summary -->
        <div class="col-md-4">
            <div class="card shadow-sm p-4 rounded-4">
                <h5>User Summary</h5>
                <p><strong>Total Users:</strong> @stats.TotalUsers</p>
                <p><strong>Patients:</strong> @stats.Patients</p>
                <p><strong>Clinicians:</strong> @stats.Clinicians</p>
                <p><strong>Admins:</strong> @stats.Admins</p>
            </div>
        </div>

        <!-- Manage Patients -->
        <div class="col-md-4">
            <div class="card shadow-sm p-4 rounded-4 card-hover" onclick="location.href='/Admin/Patients'">
                <h5>Manage Patients</h5>
                <p>View, edit, and remove patient accounts.</p>
            </div>
        </div>

        <!-- Manage Clinicians -->
        <div class="col-md-4">
            <div class="card shadow-sm p-4 rounded-4 card-hover" onclick="location.href='/Admin/Clinicians'">
                <h5>Manage Clinicians</h5>
                <p>View all clinicians and their details.</p>
            </div>
        </div>

    </div>

    <div class="row g-4 mt-3">

        <!-- Manage Admins -->
        <div class="col-md-4">
            <div class="card p-4 shadow-sm rounded-4" onclick="location.href='/Admin/Admins'">
                <h5>Manage Admin Accounts</h5>
                <p>Control admin accounts and privileges.</p>
            </div>
        </div>

        <!-- System Settings -->
        <div class="col-md-4">
            <div class="card p-4 shadow-sm rounded-4" onclick="location.href='/Admin/Settings'">
                <h5>Settings</h5>
                <p>System configuration and security options.</p>
            </div>
        </div>

        <!-- Logs -->
        <div class="col-md-4">
            <div class="card p-4 shadow-sm rounded-4" onclick="location.href='/Admin/Logs'">
                <h5>System Logs</h5>
                <p>Audit logs and activity history.</p>
>>>>>>> b283525eb90ca6690cffbf8e55180a5495858727
            </div>
        </div>

    </div>
</div>